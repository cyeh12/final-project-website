Check if node is installed by using 
node -v
npm create vite@latest
Selected React as framework and Typescript as variant
cd to project folder
npm install
npm run dev
Website is located at http://localhost:XXXX/

(1) Create a function component in the file name src/Message.tsx
(2) Go to App.tsx and create a App() function and imported the Message function
(3) Export default App
(4) Use a variable name and an if function
(5) Use the return keyword to return <div> element with a <Message/> tag
created before 
